import { FormControl, FormGroup } from "@angular/forms";

export class ProfileModel {

    userProfile: any = [];

    skills: any = [];

    addSkill(skill: any) {
    }

    removeSkill(skill: any) {
    }
}